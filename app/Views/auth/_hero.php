<div class="sp-cart-hero">
    <div class="sp-cart-hero-inner">
        <div class="sp-cart-hero-text">
            <div class="sp-cart-hero-eyebrow"><?= esc($eyebrow ?? 'My Account') ?></div>
            <h1><?= esc($heroTitle ?? '') ?></h1>
            <p><?= esc($heroSubtitle ?? '') ?></p>
            <div class="sp-hero-pills">
                <span class="sp-hero-pill">Secure account</span>
                <span class="sp-hero-pill">Order tracking</span>
                <span class="sp-hero-pill">Saved addresses</span>
            </div>
        </div>
    </div>
</div>
